on: [push]

jobs:
  build-and-publish:
    name: Build and Publish
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Execute Action Graph
        uses: actionforge/action@v0.4.37-alpha
        with:
          graph_file: build-and-publish.yml
        secrets:
          TEST: Foo
